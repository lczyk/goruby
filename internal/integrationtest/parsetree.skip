# Skip-list for TestMRIParseTreeDiff (parsetree_test.go).
#
# Format: <phase> <version-range> <pattern>[: reason]
#
# phase   = src2-rejected | tree-mismatch
# range   = *                          (all versions)
#         | <ver>                      (single version, e.g. 3.4)
#         | <lo>..                     (lo and above)
#         | ..<hi>                     (hi and below)
#         | <lo>..<hi>                 (closed range)
# pattern = path relative to repo root; segments may be `*`
#
# Stale entries (where the test now passes) fail loudly -- prune them out.

# === auto-generated entries from initial test run ===
# Generated by extracting failures from `make integration-parsetree` on parser-work branch.
# To regenerate after fixing parser/AST bugs, prune stale entries (test will fail
# loudly on any entry where the test now passes).

# === auto-added after parser/printer fix run ===

